class Car {
  final String? type, image, speed, speed_rang, price, price_range, bg_color;

  Car({
    this.type,
    this.image,
    this.speed,
    this.speed_rang,
    this.price,
    this.price_range,
    this.bg_color,
  });
}
